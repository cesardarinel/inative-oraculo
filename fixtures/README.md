# fixtures/

Fuentes + manifest + snapshots exportados desde iNative hacia el oráculo.
Ver `documentacion/04-fixtures.md`.

- `RPGLE/invalidos/var-no-declarada.rpgle` — vertical 1: variable no
  declarada que debe producir `RNF7030`.
