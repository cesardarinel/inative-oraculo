# reportes/

Informes de conformidad del oráculo: coincidencias y divergencias entre la
simulación local y el IBM i real (ver `documentacion/09-reportes.md`). El
informe generado `report.md` no se versiona (en `.gitignore`).
