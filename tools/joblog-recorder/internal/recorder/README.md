Este paquete documenta que la lógica de captura vive en `probe/joblog_recorder.py` + `probe/colectores/{joblog_v2,job_info}.py`.
El runtime replay vive en `internal/joblog/` (JobLog Engine §22).

No duplicar: este directorio es marcador de arquitectura §2.
